USER_AGENT=Mozilla/5.0 (compatible; KidSearch-Crawler/2.0)
MEILI_URL=http://localhost:7700
MEILI_KEY=a_master_key_that_is_long_and_secure
INDEX_NAME=kidsearch
MAX_RETRIES=3
TIMEOUT=15
BATCH_SIZE=20
CACHE_DAYS=7

# --- Embeddings Configuration ---
# To enable embeddings, set EMBEDDING_PROVIDER to "gemini" or "snowflake".
# If set to "none" or not present, embeddings are disabled.
EMBEDDING_PROVIDER=none  # Options: gemini, snowflake, infloat, none
GEMINI_API_KEY=your_gemini_api_key_here  # Required if EMBEDDING_PROVIDER=gemini
EMBEDDING_MODEL=intfloat/multilingual-e5-base  # Model for snowflake provider
# Snowflake options: snowflake-arctic-embed-xs (22M, 384dim), -s (33M, 384dim), -m (110M, 768dim), -l (335M, 1024dim)

# --- Parallelism Settings ---
CONCURRENT_REQUESTS=5    # Nombre de pages crawlées en parallèle
MAX_CONNECTIONS=100      # Pool de connexions HTTP
DEFAULT_DELAY=0.5        # Délai minimum entre requêtes
MAX_WORKERS=20           # Maximum absolute number of workers to prevent resource exhaustion

# --- API Backend Configuration (optional) ---
API_ENABLED=false        # Enable FastAPI backend server
API_HOST=0.0.0.0        # API server host
API_PORT=8080           # API server port
API_WORKERS=4           # Number of Uvicorn workers

# --- Dashboard Security ---
# To protect the dashboard with a password, create a file .streamlit/secrets.toml
# and add: DASHBOARD_PASSWORD = "your_strong_password"

# Docker Service Mode (for unified Docker deployment)
SERVICE=all             # Options: all (dashboard+api), dashboard, api